# 2026-04-07

## Supabase Setup
- Anon key and service_role key saved to `.secrets/supabase.env`
- Project ref: `dyzkfmdjusdyjmytgeah` (confirmed correct — URL in anon key JWT matches)
- MCP URL: `https://mcp.supabase.com/mcp?project_ref=dyzkfmdjusdyjmytgeah`
- Working on: finishing Supabase MCP setup (was stuck on anon key retrieval)

## Contabo — HARD RULES
- **vmi3211554** = designated test VPS (Instance ID: 203211554, IP: 144.91.86.170)
  - Already provisioned and running from API test on 2026-04-06
  - **Do NOT re-create this via API** — it already exists
- **vmi3094584** = production VPS (the main Contabo VPS we're on)
- **NEVER create new VPS instances for testing** — any API call that provisions a new instance = charges
- **Always ask Pulkit for explicit approval** before provisioning anything on Contabo
- The numeric ID issue (secretId format) was causing accidental creates — avoid that pattern

## Context
- Session was compacted during Supabase MCP work
- Pulkit confirmed he had already given all Supabase credentials
- PinchTab is DEPRECATED — using Chrome VNC (Docker, port 5800) instead
